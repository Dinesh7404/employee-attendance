# Backend Environment Variables Example
# Copy this file to .env (do NOT commit .env)

# MongoDB Atlas connection string (replace <PASSWORD> with your actual password)
MONGODB_URI=mongodb+srv://dinesh:6650@cluster0.0kyxynq.mongodb.net/attendance?retryWrites=true&w=majority&appName=Cluster0

# JWT secret key (generate a long random string)
JWT_SECRET=replace_with_long_random_hex_or_base64

# Frontend URL for CORS
FRONTEND_URL=https://employee-shiftlog.vercel.app

# Environment
NODE_ENV=production
PORT=5000

# DB retry logic (optional overrides)
DB_MAX_RETRIES=5
DB_RETRY_DELAY_MS=2000
